rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Versão atual: Firebase é usado somente para autenticação.
    // Os dados do Projeto Leve permanecem no aparelho.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
