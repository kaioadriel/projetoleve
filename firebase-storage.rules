rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Versão atual: fotos permanecem localmente no IndexedDB.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
