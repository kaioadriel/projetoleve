rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Fotos são armazenadas localmente no IndexedDB, sem compressão.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
